/*kineval.setpointDanceSequence = function execute() {
  if (!kineval.params.update_pd_dance) return;

  var curdate = new Date();
  for (x in robot.joints) {
      kineval.params.setpoint_target[x] = kineval.params.dance_pose_index[x];
  }
}
*/
